# Local PostgreSQL connection for the public demo.
DATABASE_URL=postgresql://postgres:postgres@localhost:5432/fleet_demo

# Use a different database for automated tests when needed.
DATABASE_URL_TEST=postgresql://postgres:postgres@localhost:5432/fleet_demo_test

# Development-only placeholder. Replace locally; never commit a real secret.
SESSION_SECRET=replace-with-a-local-development-secret
