# Port used by the public demo API and production server.
PORT=5000

# This public edition currently runs on synthetic in-memory data.
# No production database URL, session secret or customer configuration is required.
